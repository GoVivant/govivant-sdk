export default class Category {
    name: string
    type: string
    image: string

    constructor(name: string, type: string, image: string) {
        this.name = name
        this.type = type
        this.image = image
    }
}