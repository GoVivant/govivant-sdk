export default class Ingredient {
    name: string;
    product_id: string;
    business_id: string;
    image: string;
    rank: number;
    constructor(name: string, product_id: string, business_id: string, image: string, rank: number);
}
