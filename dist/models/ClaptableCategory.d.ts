export default class ClaptableCategory {
    name: string;
    image: string;
    rank: number;
    business_id: string;
    enabled: boolean;
    constructor(name: string, image: string, rank: number, business_id: string);
}
